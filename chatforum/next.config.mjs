/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,  // Disable Strict Mode in React for development
  
    // Other configurations can be added here if needed
  };
  
  export default nextConfig;